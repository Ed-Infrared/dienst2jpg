# CLAUDE.md Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a CLAUDE.md file that provides guidance to Claude Code when working with this repository, including common commands, development workflow, and repository structure.

**Architecture:** Analyze the existing repository structure (PDF timetable files) and create a concise CLAUDE.md file that outlines essential information for working with this codebase.

**Tech Stack:** None (repository contains only PDF files)

---

### Task 1: Analyze Repository Structure

**Files:**
- Modify: `CLAUDE.md` (new file)

- [ ] **Step 1: Examine repository contents**

```bash
ls -la
ls -la diensten/
```

- [ ] **Step 2: Check for existing documentation**

```bash
find . -name "*.md" -o -name "README*" -o -name "doc*" | head -20
```

- [ ] **Step 3: Check git status and configuration**

```bash
git status
git log --oneline -5
git config --list | grep -i claude
```

- [ ] **Step 4: Commit initial analysis**

```bash
git add .
git commit -m "docs: analyze repository structure for CLAUDE.md creation"
```

### Task 2: Create CLAUDE.md File

**Files:**
- Create: `CLAUDE.md`

- [ ] **Step 1: Write CLAUDE.md header and introduction**

```markdown
# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.
```

- [ ] **Step 2: Add repository overview section**

```markdown
## Repository Overview

This repository contains public transportation timetable PDFs for Zaandam:
- `ZAANDAM_MA-VR.pdf`: Monday-Friday schedule
- `ZAANDAM_ZATERDAG.pdf`: Saturday schedule
- `ZAANDAM_ZONDAG.pdf`: Sunday schedule
```

- [ ] **Step 3: Add development workflow section**

```markdown
## Development Workflow

### Common Commands
- View timetable PDFs: Use any PDF viewer
- Add new timetables: Copy PDF files to the `diensten/` directory
- Remove outdated timetables: Delete files from `diensten/`

### Git Workflow
- Commit messages in English, imperative mood (e.g., "Add timetable" not "Added timetable")
- All new files should be added to git for tracking (except sensitive information)
- Before writing files, always read existing files first
- If files don't exist, create empty files first
```

- [ ] **Step 4: Add file conventions section**

```markdown
## File Conventions

### PDF Files
- Store all timetable PDFs in the `diensten/` directory
- Use descriptive filenames indicating location and schedule type
- Maintain consistent naming pattern: `LOCATION_TYPE.pdf`

### Documentation
- Keep documentation in English for code comments
- Respond to user in Dutch as per global preferences
```

- [ ] **Step 5: Commit the CLAUDE.md file**

```bash
git add CLAUDE.md
git commit -m "docs: create CLAUDE.md with repository guidance"
```

### Task 3: Verify and Finalize

**Files:**
- Modify: `CLAUDE.md` (if needed)

- [ ] **Step 1: Review created CLAUDE.md**

```bash
cat CLAUDE.md
```

- [ ] **Step 2: Check final git status**

```bash
git status
```

- [ ] **Step 3: Final commit if changes needed**

```bash
git add CLAUDE.md
git commit -m "docs: finalize CLAUDE.md content"
```